apiVersion: v1
kind: Service
metadata:
  name: swiggy-lb-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: swiggy-app-green  # This is the key: switching traffic to green
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
